# i18n Module

**Stack:** i18next + react-i18next + i18next-browser-languagedetector

---

## Supported Locales

| Code | Language | Primary Market |
|---|---|---|
| `en` | English | US / Canada (default) |
| `pt-BR` | Brazilian Portuguese | Brazil |
| `es` | Spanish | Latin America |

---

## Files (Story 1.3 ✓)

| File | Description |
|---|---|
| `src/i18n/index.ts` | i18next init — LanguageDetector + react-i18next, bundled resources |
| `src/i18n/index.test.ts` | Tests: supportedLngs, fallback, 11 required key sets across all 3 locales |
| `src/i18n/LanguageSwitcher.tsx` | EN/PT-BR/ES button group; handles full locale change flow |
| `src/i18n/LanguageSwitcher.test.tsx` | Tests: renders, aria-current, click flow, aria-label |
| `src/i18n/locales/en/translation.json` | English strings — all 11 top-level key namespaces |
| `src/i18n/locales/pt-BR/translation.json` | PT-BR strings — identical key structure |
| `src/i18n/locales/es/translation.json` | Spanish strings — identical key structure |

Story 7.2 adds placeholder SEO keys under `seo.landing`, `seo.demo`, and `seo.dashboard.{home,recovery,payouts,insights,settings}` in all three locale files so new scaffold routes never render raw translation keys in `<title>` or OG metadata before full Story 7.5 extraction.

---

## Detection Order

```
localStorage ('i18nextLng') → browser preference → fallback: 'en'
```

---

## Translation Key Structure (dot-nested, max 3 levels)

11 required top-level namespaces: `nav`, `hero`, `syncrevenue`, `services`, `comparison`, `team`, `security`, `references`, `privacy`, `forms`, `errors`

```json
{
  "nav": { "demo": "...", "contact": "..." },
  "hero": { "headline": "...", "cta": "...", "stats": { "assertivity": "..." } },
  "forms": {
    "demo": { "name": "...", "emailError": "...", "submit": "..." },
    "contact": { "subject": "...", "submit": "..." }
  }
}
```

---

## Locale Change Flow (always this order — enforced by arch spec)

```typescript
i18next.changeLanguage(locale)          // 1. update i18next (triggers re-renders)
useLocaleStore.setState({ locale })      // 2. update Zustand store
localStorage.setItem('i18nextLng', locale) // 3. explicit persist
```

---

## Rules

- Source locale in components from `useLocaleStore` — never directly from `i18next.language`
- `import './i18n'` must be the FIRST import in `main.tsx` before React
- `main.tsx` syncs Zustand store to i18next-detected locale at startup
- `useSuspense: false` — resources are bundled, not async-fetched
- All validation errors locale-aware (FR16, FR20)
- Only `en`, `pt-BR`, `es` allowlisted server-side (FR40, Story 2.7)

---

## Status

| Story | Files Created |
|---|---|
| 1.3 ✓ | `index.ts`, `index.test.ts`, `LanguageSwitcher.tsx`, `LanguageSwitcher.test.tsx`, all 3 locale JSONs |
| 7.2 ✓ | SEO metadata keys for `/v2`, `/demo`, and all `/dashboard/*` placeholders added to all 3 locale JSONs |
